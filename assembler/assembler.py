def mips_assembler(instructions_list):
    for s in instructions_list:
        pass


def instructions(opcode):
    if opcode == "add":
        op = bin(0)
        shamt = bin(0)
        funct = bin(32)
